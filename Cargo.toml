[workspace]
members = [
    "kernel-entry",
    "kernel",
    "device",
    "fatfs",
    "linux-syscall",
    "task_scheduler"
]
# default-members = ["kernel"]

[profile.release]
lto = true
debug = true